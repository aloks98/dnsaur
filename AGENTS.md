# dnsaur — notes for agents

## Agent skills

### Issue tracker

Forgejo Issues on git.nexus.e412.in, via the `fj` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default labels, unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
