// Placeholder — a later task replaces this with real password/TOTP/API
// token management.
export function Account() {
  return (
    <div>
      <h1 className="text-2xl font-heading font-semibold text-foreground">Account</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Password, TOTP, and API token management land here.
      </p>
    </div>
  );
}
