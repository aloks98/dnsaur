// Placeholder — a later task replaces this with real block/allow list,
// group, and rule management.
export function Filtering() {
  return (
    <div>
      <h1 className="text-2xl font-heading font-semibold text-foreground">Filtering</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Block/allow lists, groups, and rules land here.
      </p>
    </div>
  );
}
