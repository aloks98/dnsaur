// Placeholder — a later task replaces this with real local DNS record
// management (A/AAAA/CNAME/TXT).
export function LocalDns() {
  return (
    <div>
      <h1 className="text-2xl font-heading font-semibold text-foreground">Local DNS</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Custom A/AAAA/CNAME/TXT records land here.
      </p>
    </div>
  );
}
