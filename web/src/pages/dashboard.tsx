// Placeholder — a later task replaces this with the real dashboard
// (query volume, block rate, top clients, timeline).
export function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-heading font-semibold text-foreground">Dashboard</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Query volume, block rate, and top clients land here.
      </p>
    </div>
  );
}
